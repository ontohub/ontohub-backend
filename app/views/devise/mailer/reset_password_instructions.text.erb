Hello <%= @resource.display_name || @resource.to_param %>!

Someone has requested a link to change your password. You can do this by visiting the following URL:

<%= edit_password_url(@resource, reset_password_token: @token) %>

Your reset password token is: <%= @token%>

If you didn't request this, please ignore this email.
Your password won't change until you access the URL above and create a new one.
