{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "V2 Users Passwords Controller: POST/resend_password_recovery_email",
  "description": "V2 Users Passwords Controller: POST/resend_password_recovery_email",

  "type": "object",
  "required": ["meta", "jsonapi"],
  "properties": {
    "meta": {
      "type": "object",
      "required": ["action"],
      "properties": {
        "action": {
          "description": "The action that has been performed",
          "example": "An email with instructions to reset the password has been sent to ada@example.com if a user is registered by this email address.",
          "type": "string",
          "pattern": "^An email with instructions to reset the password has been sent to [^@]+@[^@]+ if a user is registered by this email address\\.$"
        }
      }
    },
    "jsonapi": { "$ref": "../../../../controllers/v2/jsonapi_toplevel_object.json" }
  }
}
