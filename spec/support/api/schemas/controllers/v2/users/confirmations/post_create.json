{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "V2 Users Confirmations Controller: POST/create",
  "description": "V2 Users Confirmations Controller: POST/create",

  "type": "object",
  "required": ["meta", "jsonapi"],
  "properties": {
    "meta": {
      "type": "object",
      "required": ["action"],
      "properties": {
        "action": {
          "description": "The action that has been performed",
          "example": "A new confirmation email has been sent to ada@example.com if a user is registered by this email address.",
          "type": "string",
          "pattern": "^A new confirmation email has been sent to [^@]+@[^@]+ if a user is registered by this email address\\.$"
        }
      }
    },
    "jsonapi": { "$ref": "../../../../controllers/v2/jsonapi_toplevel_object.json" }
  }
}
